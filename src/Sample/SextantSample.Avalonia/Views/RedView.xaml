<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:SextantSample.ViewModels;assembly=SextantSample.ViewModels"
             d:DataContext="{d:DesignInstance viewModels:RedViewModel, IsDesignTimeCreatable=False}"
             x:Class="SextantSample.Avalonia.Views.RedView"
             mc:Ignorable="d" Background="#ffffff">
    <StackPanel Background="Red">
        <Button Content="Pop Modal"
                Foreground="White"
                Command="{Binding PopModal}"/>
        <Button Content="Push Another Red Page"
                Foreground="White"
                Command="{Binding PushPage}"/>
        <Button Content="Pop Page"
                Foreground="White"
                Command="{Binding PopPage}"/>
        <Button Content="Pop To Root"
                Foreground="White"
                Command="{Binding PopToRoot}"/>
    </StackPanel>
</UserControl>